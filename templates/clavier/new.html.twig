{% extends 'base.html.twig' %}

{% block title %}{% trans %}titre_nouveau_clavier{% endtrans %}{% endblock %}

{% block metas %}
    <meta name="turbo-cache-control" content="no-cache">
{% endblock %}

{% block body %}
    {% if not is_granted('ROLE_ADMIN') %}
        <div>{% trans %}msg_acces_page_invalide{% endtrans %}</div>
    {% else %}

        <h1>{% trans %}titre_nouveau_clavier{% endtrans %}</h1>

        {{ include('clavier/_form.html.twig') }}

        <a href="{{ path('app_clavier_index') }}">{% trans %}texte_btn_retour{% endtrans %}</a>
    {% endif %}
{% endblock %}

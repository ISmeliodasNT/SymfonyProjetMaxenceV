<form method="post" action="{{ path('app_clavier_delete', {'id': clavier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clavier.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Modifier souris{% endblock %}

{% block body %}

    {% if not is_granted('ROLE_ADMIN') %}
        <div>Vous n'avez pas accès à cette page.</div>
    {% else %}

        <h1>Edit Souris</h1>

        {{ include('souris/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

        <a href="{{ path('app_souris_index') }}">retour</a>

        {{ include('souris/_delete_form.html.twig') }}

    {% endif %}
{% endblock %}

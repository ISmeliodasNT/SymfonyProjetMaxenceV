<!DOCTYPE html>

<title>Edit Souris</title>

{% block body %}
    <h1>Edit Souris</h1>

    {{ include('souris/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_souris_index') }}">back to list</a>

    {{ include('souris/_delete_form.html.twig') }}
{% endblock %}

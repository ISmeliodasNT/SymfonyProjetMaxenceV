<form method="post" action="{{ path('app_souris_delete', {'id': souri.id}) }}" onsubmit="return confirm('texte_btn_confirmation_supprimer'|trans);">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ souri.id) }}">
    <button class="btn">{% trans %}texte_btn_supprimer{% endtrans %}</button>
</form>
